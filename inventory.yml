---
# Alternatively, you can use groups:
turtles:
  raspberry_pi:
    hosts:
      - leonardo:
      - donatello:
      - raphael:
      - michealangelo:
    vars:
      ansible_python_interpreter: /usr/bin/python3
